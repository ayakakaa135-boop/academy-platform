# قائمة المهام الأمنية الإضافية (Security Roadmap) 🛡️

هذه القائمة تحتوي على تحسينات أمنية مقترحة لتعزيز حماية منصة الأكاديمية بعد إطلاقها.

## 1. حماية لوحة التحكم (Admin Security)
- [ ] **تغيير مسار الإدارة**: تغيير `/admin/` إلى رابط سري وغير متوقع في ملف `config/urls.py`.
- [ ] **تفعيل التحقق الثنائي (2FA)**: إضافة طبقة حماية إضافية لحسابات الإدارة باستخدام مكتبة مثل `django-two-factor-auth`.
- [ ] **تقييد الوصول بـ IP**: (اختياري) حصر الوصول للوحة التحكم على عناوين IP محددة إذا كان فريق الإدارة ثابتاً.

## 2. أمان البيانات والملفات (Data & File Security)
- [ ] **تثبيت django-cleanup**: لضمان حذف الصور والملفات القديمة من السيرفر تلقائياً عند تحديثها أو حذفها.
- [ ] **فحص الملفات المرفوعة**: التأكد من أن ملفات الـ Media لا تملك صلاحيات التنفيذ (Execution Permissions) على مستوى السيرفر.
- [ ] **تشفير البيانات الحساسة**: التأكد من تشفير أي بيانات حساسة إضافية في قاعدة البيانات إذا لزم الأمر.

## 3. الحماية من الهجمات (Attack Prevention)
- [ ] **تفعيل Rate Limiting**: استخدام `django-ratelimit` على روابط تسجيل الدخول، استعادة كلمة المرور، وروابط الدفع لمنع هجمات Brute Force و DoS.
- [ ] **إعداد رؤوس أمان إضافية (HSTS)**: تفعيل `SECURE_HSTS_SECONDS` في `settings.py` لفرض اتصال HTTPS بشكل دائم.
- [ ] **مراقبة السجلات (Logging)**: إعداد نظام تنبيهات عند حدوث أخطاء متكررة في الـ Webhooks أو محاولات دخول فاشلة.

## 4. أمان الطرف الثالث (Third-party Security)
- [ ] **تحديث المكتبات دورياً**: مراجعة وتحديث ملف `requirements.txt` بانتظام لسد أي ثغرات تظهر في Django أو Stripe.
- [ ] **مراجعة صلاحيات API Keys**: التأكد من أن مفاتيح Stripe المستخدمة تملك فقط الصلاحيات اللازمة (Principle of Least Privilege).

---
*تم إعداد هذه القائمة بناءً على الفحص الأمني السريع بتاريخ 13 فبراير 2026.*
